paths:
  cache: storage/cache
  config: app/config
  database: storage/database
  web: public
  themebase: public/theme
  files: public/uploads
  view: public/assets/admin

urls:
  app: /
  view: assets/admin
